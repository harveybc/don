{# Creation view template #}
{# harveybc 20171014 #}
{# Includes the master_AdminLTE template containing AdminLTE #}
{% extends 'master_AdminLTE' %}{% block content %}
    <!-- form start -->
    <form class="form-horizontal" method="post" action="/applications">
        <!-- form contents. Include path is relative to nonjunks config directory in resource/views -->
        {% include "applications/form.njk" %}
        <!-- form end -->
    </form>
{% endblock %}
{% block footer %}
    <div id="helpModal" class="modal fade" role="dialog" >
        <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Applications Creation View Help</h4>
                </div>
                <div class="modal-body">
                    <p>This view allows to Create a new applications register, fill the details and clik on the save button on the top or the bottom.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div> 
    <!-- End Modal Help Dialog -->
    <script type="text/javascript">
        // when page finishes loading, sets the values to the ones from data
        $(document).ready(function () {
            // Shows Help When button clicked
            $('#btn-trig').click(function () {
                $('#helpModal').modal('show');
            });
            // initialize menu start active item
            document.getElementById("m_applications").classList.add('active');

            // Initialize values of form
            /*          // Status 
             $('#inputDifficulty').val(data[0].difficulty);
             $('#inputLastApplicationId').val(data[0].last_application_hash);
             $('#inputLastApplicationDate').val(data[0].last_application_date);
             $('#inputLastApplicationDifficulty').val(data[0].last_application_difficulty);
             $('#inputLastApplicationTime').val(data[0].last_application_time);
             $('#inputLastApplicationSize').val(data[0].last_application_size);
             // Optional status for LAST APPLICATION if using application_time_control>0 (!OPoW) 
             $('#inputLastApplicationPerformance').val(data[0].last_application_size);
             // Optional status for LAST OPTIMUM in current application if using application_time_control>0 (!OPoW) 
             $('#inputLastOptimumId').val(data[0].last_optimum_id);
             $('#inputLastOptimumDate').val(data[0].last_optimum_date);
             $('#inputLastOptimumPerformance').val(data[0].last_optimum_date);          
             */
        });
    </script>

{% endblock %}
