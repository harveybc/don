{# Creation view template #}
{# harveybc 20171014 #}
{# Includes the master_AdminLTE template containing AdminLTE #}
{% extends 'master_AdminLTE' %}{% block content %}
    <!-- form start -->
    <form class="form-horizontal" method="post" action="/datasets">
    <!-- form contents. Include path is relative to nonjunks config directory in resource/views -->
    {% include "datasets/form.njk" %}
    <!-- form end -->
    </form>
{% endblock %}
{% block footer %}
    <div id="helpModal" class="modal fade" role="dialog" >
        <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Datasets Creation View Help</h4>
                </div>
                <div class="modal-body">
                    <p>This view allows to Create a new datasets register, fill the details and clik on the save button on the top or the bottom.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div> 
    <!-- End Modal Help Dialog -->
    <script type="text/javascript">
                // when page finishes loading, sets the values to the ones from data
                $(document).ready(function () {
            // Shows Help When button clicked
            $('#btn-trig').click(function () {
                $('#helpModal').modal('show');
            });
                        // initialize menu start active item
            document.getElementById("m_optimization").classList.add('menu-open');
            document.getElementById("m_optimization").classList.add('active');            
            document.getElementById("m_datasets").classList.add('active');

            // Initialize values of form
/*          // Status 
            $('#inputDifficulty').val(data[0].difficulty);
            $('#inputLastDatasetId').val(data[0].last_dataset_hash);
            $('#inputLastDatasetDate').val(data[0].last_dataset_date);
            $('#inputLastDatasetDifficulty').val(data[0].last_dataset_difficulty);
            $('#inputLastDatasetTime').val(data[0].last_dataset_time);
            $('#inputLastDatasetSize').val(data[0].last_dataset_size);
            // Optional status for LAST DATASET if using dataset_time_control>0 (!OPoW) 
            $('#inputLastDatasetPerformance').val(data[0].last_dataset_size);
            // Optional status for LAST OPTIMUM in current dataset if using dataset_time_control>0 (!OPoW) 
            $('#inputLastOptimumId').val(data[0].last_optimum_id);
            $('#inputLastOptimumDate').val(data[0].last_optimum_date);
            $('#inputLastOptimumPerformance').val(data[0].last_optimum_date);          
*/
        });
    </script>

{% endblock %}
